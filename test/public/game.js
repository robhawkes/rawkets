describe("Game", function() {
	var game;

	// Gamepad
	describe("Initialisation", function() {
		beforeEach(function() {
			game = new r.Game();
		});

		it("Should do something on initialisation", function() {
			assert(1 === 0);
		});
	});
});