/**************************************************
** CONNECTION MANAGER
**************************************************/

var io = require("socket.io");

var Connection = exports.Connection = function (options) {
	var self = this;
};

Connection.prototype.init = function(app) {
	this.socket = io.listen(app);
};